PROJECT(web_server)
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
ADD_DEFINITIONS(-Wall -g -O0 -std=c++11)
AUX_SOURCE_DIRECTORY(. DIR_SRCS)
ADD_EXECUTABLE(web_server ${DIR_SRCS})
TARGET_LINK_LIBRARIES(web_server pthread)
